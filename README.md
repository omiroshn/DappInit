# DappInit

To run smart contracts on private blockchain you should install ganache-cli:
```
npm install -g ganache-cli
```
And then run it with the command:
```
$HOME/.npm-global/bin/ganache-cli
```

That's it!
Then you can open Remix IDE and connect to your server
https://remix.ethereum.org/
